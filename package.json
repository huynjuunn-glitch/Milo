{
  "name": "seller-operations-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "postbuild": "npm run check:site && npm run test:seo",
    "preview": "astro preview",
    "check": "astro check",
    "check:site": "node scripts/check-site.mjs",
    "test": "node --experimental-strip-types scripts/test-images.mjs",
    "test:workspace": "node --experimental-strip-types scripts/test-workspace.mjs",
    "test:format-compare": "node --experimental-strip-types scripts/test-format-compare.mjs",
    "test:seo": "node --experimental-strip-types scripts/test-seo.mjs",
    "measure:format": "node scripts/measure-format-fixture.mjs",
    "generate:images": "node --experimental-strip-types scripts/generate-preview-images.mjs",
    "quality": "npm test && npm run build && npm run test:workspace && npm run test:format-compare"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.4",
    "fflate": "^0.8.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "happy-dom": "^20.14.0",
    "prettier": "^3.9.6",
    "prettier-plugin-astro": "^0.14.1",
    "sharp": "^0.35.3",
    "typescript": "^6.0.3"
  }
}
